# nova.toml — Polaris example: polaris-example-07-sse-stream

[package]
name = "polaris-example-07-sse-stream"
version = "0.1.0"
description = "Server-Sent Events ticker via StreamBody"

[dependencies]
polaris = { path = "../.." }
